# PROTAC-STAN Training and Predictions

This directory contains scripts for training and collect predictions from PROTAC-STAN models on the TACK dataset.

The directory `predictions/` contains the predictions of PROTAC-STAN models on the 5×5 cross-validation splits and the hold-out set, which can be used for comparison with other models.
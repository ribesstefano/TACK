# Dependencies for building this MkDocs site (docs/, mkdocs.yml).
# Deliberately separate from pyproject.toml: mkdocstrings statically parses
# tackai's source via griffe, so building the docs needs none of the
# project's heavy ML dependencies (torch, xgboost, rdkit, ...) installed.
mkdocs>=1.6
mkdocs-material>=9.5
mkdocstrings[python]>=0.26
mkdocs-jupyter>=0.25
